all:
  vars:
    ansible_user: ansible
    ansible_python_interpreter: /usr/bin/python3
    # DNS de la passerelle vmbr0 (forwarde vers le DNS campus)
    # Fallback 1.1.1.1 configure dans le daemon Docker du playbook
    dns_server: "172.16.20.1"
    dns_search: "campus12avenue.fr"
  hosts:
    app:
      ansible_host: "172.16.20.35"
